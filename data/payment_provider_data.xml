<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="payment_provider_pagoplux" model="payment.provider">
            <field name="name">PagoPlux</field>
            <field name="code">pagoplux</field>
            <!-- <field name="display_as">Tarjeta de Crédito (powered by Pagoplux)</field> -->
            <field name="image_128" type="base64" file="payment_pagoplux/static/description/icon.png"/>
            <field name="provider">pagoplux</field>
            <field name="company_id" ref="base.main_company"/>
            <!-- <field name="payment_flow">form</field> -->
            <!-- <field name="module_id" ref="base.module_payment_pagoplux"/> -->                   
            <!-- We are not able to process your payment. Error while render the template -->
            <field name="inline_form_view_id" ref="inline_form_pagoplux" />  
            <!-- <field name="redirect_form_view_id" ref="inline_form_pagoplux" /> -->         
            <!-- <field name="state">test</field> -->
            <field name="pre_msg"><![CDATA[<p>Pagoplux</p>]]></field>
            <field name="paybox_remail">correo_establecimiento</field>
            <field name="paybox_rename">nombre_establecimiento</field>
            <field name="payment_method_ids" 
                   eval="[Command.set([
                         ref('payment_method_pagoplux'),
                     ])]"/>
        </record>
    </data>
</odoo>